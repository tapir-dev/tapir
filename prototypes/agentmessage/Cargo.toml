# PROTOTYPE — throwaway, do not merge to main. Answers wayfinder ticket #4.
[package]
name = "proto-agentmessage"
version = "0.0.0"
edition = "2024"
publish = false

[dependencies]
tapir-provider = { path = "../../../../../../tapir-provider" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
