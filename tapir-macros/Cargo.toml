[package]
name = "tapir-macros"
version = "0.1.0"
edition = "2024"
license = "ISC"
description = "The #[tool] proc-macro for tapir, re-exported through tapir"

[lib]
proc-macro = true

[dependencies]
syn = { version = "2", features = ["full"] }
quote = "1"
proc-macro2 = "1"
